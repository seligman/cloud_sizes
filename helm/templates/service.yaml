apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud_sizes_exporter.fullname" . }}
  labels:
    {{- include "cloud_sizes_exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloud_sizes_exporter.selectorLabels" . | nindent 4 }}
